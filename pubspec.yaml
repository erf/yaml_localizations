name: yaml_localizations
description: A minimal YAML localization package built on LocalizationsDelegate.
version: 1.0.0
homepage: https://github.com/erf/yaml_localizations

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  yaml: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

